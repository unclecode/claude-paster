export const DEFAULT_SETTINGS = {
    lengthThreshold: 3999,
    openDelimiter: '```',
    closeDelimiter: '```',
    useDelimiters: true,
    escapeDelimiters: true,
    forceDelimiters: false,
    multilineOnly: true,
    autoRecover: true,
    recoveryInterval: 1000,
    enabled: true,            // Global extension toggle
    allowDefault: false,      // Allow default paste behavior after our handling
};